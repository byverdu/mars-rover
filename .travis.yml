language: node_js
node_js:
  - "10"

env:
  global:
    - NODE_ENV=production

before_install:
  - pip install --user codecov
  - yarn global add lerna

before_script:
  - yarn install
  - lerna bootstrap

after_success:
  - codecov

script:
  - lerna run test:api