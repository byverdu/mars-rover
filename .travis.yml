language: node_js
node_js:
  - "10"

env:
  global:
    # - NODE_ENV=production

addons:
  apt:
    packages:
      # Ubuntu 16+ does not install this dependency by default, so we need to install it ourselves
      - libgconf-2-4

services:
  - docker

before_script:
  - docker-compose up --build -d

before_install:
  - pip install --user codecov
  - yarn global add lerna

after_success:
  - codecov
  - docker stop $(docker ps -aq)
  - docker rm $(docker ps -aq)

script:
  - yarn install
  - lerna run test:api

# CODECOV_TOKEN="c9b582b5-202a-4f95-a576-ababde1f1e50"